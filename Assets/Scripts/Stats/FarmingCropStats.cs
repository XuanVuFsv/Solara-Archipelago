using UnityEngine;

namespace VitsehLand.Scripts.Stats
{
    public class FarmingCropStats : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}