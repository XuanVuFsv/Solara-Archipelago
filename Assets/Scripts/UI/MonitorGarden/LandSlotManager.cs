namespace VitsehLand.Scripts.UI.MonitorGarden
{
    public class LandSlotManager : SlotManager
    {

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}