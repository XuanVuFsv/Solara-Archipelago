namespace VitsehLand.Scripts.UI.MonitorGarden
{
    public class LandSlotState : SlotState
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}